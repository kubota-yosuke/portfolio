<%= search_form_for @q, url: url do |f| %>
  <div class="input-group mb-3">
    <%= f.search_field :title_or_place_cont, class: 'form-control rounded-pill', placeholder: '日時' %>
    <%= f.search_field :title_or_place_cont, class: 'form-control rounded-pill', placeholder: '場所' %>
    <%= f.search_field :title_or_place_cont, class: 'form-control rounded-pill', placeholder: 'フリーワード' %>
    <div class="input-group-append">
      <%= button_tag :type => "submit", :class =>"btn btn-primary rounded-circle" do %>
        <i class="fa fa-search"></i>
      <% end %>
    </div>
  </div>
<% end %>
