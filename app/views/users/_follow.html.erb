<div id="follow_form">
  <% if current_user.following? @user %>
    <%= link_to "気になる削除", {controller: :users,action: :unfollow,user_id: @user.id}, method: :put %>
  <% else %>
    <%= link_to "気になる", {controller: :users,action: :follow,user_id: @user.id}, method: :put %>
  <% end %>
</div>